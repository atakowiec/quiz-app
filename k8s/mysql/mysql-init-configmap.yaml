apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-init-sql
  namespace: quiz-app
data:
  init.sql: |
    CREATE DATABASE IF NOT EXISTS quiz_database;
    USE quiz_database;

    CREATE USER 'quiz_user'@'%' IDENTIFIED BY 'xx';
    GRANT ALL PRIVILEGES ON quiz_database.* TO 'quiz_user'@'%';
    FLUSH PRIVILEGES;
