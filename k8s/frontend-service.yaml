apiVersion: v1
kind: Service
metadata:
  name: quiz-frontend
  namespace: quiz-app
spec:
  selector:
    app: quiz-frontend
  ports:
    - port: 5173
      targetPort: 80
      protocol: TCP
      name: http
  type: ClusterIP
