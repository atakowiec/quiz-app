apiVersion: v1
kind: Service
metadata:
  name: image-server
  namespace: quiz-app
spec:
  selector:
    app: image-server
  ports:
    - port: 80
      targetPort: 80
      name: http
  type: ClusterIP
