apiVersion: v1
kind: Service
metadata:
  name: quiz-backend
  namespace: quiz-app
spec:
  selector:
    app: quiz-backend
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  type: ClusterIP
